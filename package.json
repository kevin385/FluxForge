{
  "name": "fluxforge",
  "packageManager": "yarn@4.0.2",
  "private": true,
  "scripts": {
    "dev:web": "cd packages/web && yarn dev",
    "dev:api": "cd packages/web && yarn start:dev",
    "prettier": "yarn prettier . --write"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "prettier": "3.1.1",
    "typescript": "^5.3.3"
  }
}
